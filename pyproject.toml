[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "moku"
version = "0.2.0"
description = "Go board (goban) detector for photo-to-SGF conversion in Kaya"
authors = [{ name = "Hadrien Mary", email = "hadrien.mary@gmail.com" }]
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["I"]

[project.scripts]
moku = "moku.cli:app"
